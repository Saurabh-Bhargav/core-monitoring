apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sample-node-app
  namespace: monitoring
  labels:
    release: monitoring-stack
spec:
  selector:
    matchLabels:
      app: sample-node-app
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: http-metrics
      path: /metrics
      interval: 30s
